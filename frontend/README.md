# 🎵 Music System Frontend

ระบบจัดการเพลงที่พัฒนาด้วย **React TypeScript** และ **Ant Design** สำหรับวิชา System Analysis and Design 1/68

## 📋 สารบัญ

- [ภาพรวมระบบ](#ภาพรวมระบบ)
- [คุณสมบัติหลัก](#คุณสมบัติหลัก)
- [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
- [การติดตั้ง](#การติดตั้ง)
- [โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
- [การใช้งาน](#การใช้งาน)
- [การพัฒนา](#การพัฒนา)
- [การ Deploy](#การ-deploy)
- [ผู้พัฒนา](#ผู้พัฒนา)

## 🎯 ภาพรวมระบบ

Music System Frontend เป็นแอปพลิเคชันเว็บที่ให้บริการฟังเพลงออนไลน์ โดยมีผู้ใช้งาน 2 ประเภท:

### 👥 ประเภทผู้ใช้งาน

1. **สมาชิก (Member)**

   - ฟังเพลงออนไลน์
   - สร้างและจัดการเพลย์ลิสต์ส่วนตัว
   - ให้คะแนนเพลง
   - ดูประวัติการฟัง

2. **ผู้สร้าง (Creator)**
   - อัปโหลดและจัดการเพลง
   - ดูสถิติการฟังเพลง
   - จัดการข้อมูลเพลง

## ✨ คุณสมบัติหลัก

### 🎵 สำหรับสมาชิก

- **แดชบอร์ดส่วนตัว** - แสดงเพลงล่าสุด, เพลย์ลิสต์, และประวัติการฟัง
- **ฟังเพลงออนไลน์** - เล่นเพลงพร้อมบันทึกประวัติการฟัง
- **จัดการเพลย์ลิสต์** - สร้าง, แก้ไข, ลบเพลย์ลิสต์ส่วนตัว
- **ให้คะแนนเพลง** - ให้คะแนนและรีวิวเพลง
- **ค้นหาและกรอง** - ค้นหาเพลงตามชื่อ, ศิลปิน, ประเภท
- **ประวัติการฟัง** - ดูและจัดการประวัติการฟัง

### 🎼 สำหรับผู้สร้าง

- **แดชบอร์ดผู้สร้าง** - แสดงสถิติเพลง, การให้คะแนน, และการฟัง
- **จัดการเพลง** - เพิ่ม, แก้ไข, ลบเพลง
- **สถิติการฟัง** - ดูจำนวนครั้งที่ฟังและคะแนนเฉลี่ย

## 🛠️ เทคโนโลยีที่ใช้

- **React 18** - UI Framework
- **TypeScript** - Type-safe JavaScript
- **Ant Design** - UI Component Library
- **Vite** - Build Tool
- **React Router DOM** - Client-side Routing
- **Axios** - HTTP Client
- **Day.js** - Date Utility

## 🚀 การติดตั้ง

### 1. Clone Repository

```bash
git clone <repository-url>
cd music-system
```

### 2. ติดตั้ง Dependencies

```bash
npm install
```

### 3. สร้างไฟล์ Environment

สร้างไฟล์ `.env` ในโฟลเดอร์ `music-system`:

```env
VITE_API_URL=http://localhost:8088
```

### 4. รัน Development Server

```bash
npm run dev
```

แอปพลิเคชันจะรันที่ `http://localhost:5173`

## 📁 โครงสร้างโปรเจค

```
music-system/
├── public/                     # Static Assets
│   └── vite.svg
├── src/
│   ├── assets/                # Images and Static Files
│   │   ├── logo.png
│   │   └── react.svg
│   ├── components/            # Reusable Components
│   │   ├── ModernAntdWrapper.tsx
│   │   └── third-patry/
│   │       ├── Loadable.tsx
│   │       └── Loader.tsx
│   ├── hooks/                 # Custom Hooks
│   │   ├── useHistory.ts
│   │   └── useModernRef.ts
│   ├── interfaces/            # TypeScript Interfaces
│   │   ├── Creator.ts
│   │   ├── History.ts
│   │   ├── index.ts
│   │   ├── Member.ts
│   │   ├── Playlist.ts
│   │   ├── Rating.ts
│   │   ├── SignIn.ts
│   │   ├── Sound.ts
│   │   ├── SoundPlaylist.ts
│   │   └── SoundType.ts
│   ├── layout/                # Layout Components
│   │   ├── FullLayout/
│   │   │   └── index.tsx
│   │   └── MinimalLayout/
│   │       └── index.tsx
│   ├── pages/                 # Page Components
│   │   ├── authentication/
│   │   │   ├── Login/
│   │   │   │   └── index.tsx
│   │   │   └── Register/
│   │   │       └── index.tsx
│   │   ├── creator/
│   │   │   ├── dashboard/
│   │   │   │   └── index.tsx
│   │   │   └── sounds/
│   │   │       └── index.tsx
│   │   ├── Dashboard/
│   │   │   └── index.tsx
│   │   ├── history/
│   │   │   └── index.tsx
│   │   ├── Playlists/
│   │   │   ├── index.tsx
│   │   │   └── PlaylistDetail.tsx
│   │   └── Sounds/
│   │       └── index.tsx
│   ├── routes/                # Routing Configuration
│   │   ├── AdminRoutes.tsx
│   │   ├── index.tsx
│   │   └── MainRoutes.tsx
│   ├── services/              # API Services
│   │   └── https/
│   │       └── index.tsx
│   ├── utils/                 # Utility Functions
│   │   ├── dateUtils.ts
│   │   └── suppressWarnings.ts
│   ├── App.css
│   ├── App.tsx
│   ├── index.css
│   ├── main.tsx
│   └── vite-env.d.ts
├── .eslintrc.js
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 🎮 การใช้งาน

### การลงทะเบียนและเข้าสู่ระบบ

1. เข้าไปที่หน้า **ลงทะเบียน**
2. เลือกประเภทผู้ใช้งาน (สมาชิก/ผู้สร้าง)
3. กรอกข้อมูลและสร้างบัญชี
4. เข้าสู่ระบบด้วยข้อมูลที่ลงทะเบียน

### สำหรับสมาชิก

1. **ฟังเพลง**: ไปที่หน้า "เพลงทั้งหมด" และคลิกปุ่มเล่น
2. **สร้างเพลย์ลิสต์**: ไปที่หน้า "เพลย์ลิสต์" และคลิก "สร้างเพลย์ลิสต์ใหม่"
3. **เพิ่มเพลงลงเพลย์ลิสต์**: คลิกปุ่ม "เพิ่มเพลย์ลิสต์" ในหน้าเพลง
4. **ให้คะแนน**: คลิกปุ่ม "ให้คะแนน" ในหน้าเพลง
5. **ดูประวัติ**: ไปที่หน้า "ประวัติการฟัง" เพื่อดูประวัติการฟัง

### สำหรับผู้สร้าง

1. **เพิ่มเพลง**: ไปที่หน้า "จัดการเพลง" และคลิก "เพิ่มเพลงใหม่"
2. **แก้ไขเพลง**: คลิกปุ่ม "แก้ไข" ในรายการเพลง
3. **ดูสถิติ**: ดูข้อมูลในแดชบอร์ดผู้สร้าง

## 💻 การพัฒนา

### การเพิ่มหน้าใหม่

1. สร้างไฟล์ใน `src/pages/`
2. เพิ่ม route ใน `src/routes/AdminRoutes.tsx` หรือ `src/routes/MainRoutes.tsx`
3. เพิ่ม menu item ใน layout (ถ้าจำเป็น)

### การเพิ่ม Component ใหม่

1. สร้างไฟล์ใน `src/components/`
2. Export component และใช้ในหน้าที่ต้องการ

### การเพิ่ม API Service ใหม่

1. เพิ่ม interface ใน `src/interfaces/`
2. เพิ่ม API function ใน `src/services/https/index.tsx`
3. ใช้ใน component ที่ต้องการ

### การเพิ่ม Hook ใหม่

1. สร้างไฟล์ใน `src/hooks/`
2. Export hook และใช้ใน component ที่ต้องการ

## 🔧 Scripts

```bash
# Development
npm run dev          # รัน development server

# Build
npm run build        # Build สำหรับ production

# Linting
npm run lint         # ตรวจสอบ code style

# Preview
npm run preview      # Preview build result
```

## 🎨 UI Components

### Layout Components

- **FullLayout** - Layout หลักที่มี sidebar และ header
- **MinimalLayout** - Layout สำหรับหน้า authentication

### Page Components

- **Dashboard** - หน้าแดชบอร์ดหลัก
- **Sounds** - หน้าแสดงเพลงทั้งหมด
- **Playlists** - หน้าจัดการเพลย์ลิสต์
- **History** - หน้าประวัติการฟัง
- **Creator Dashboard** - แดชบอร์ดสำหรับผู้สร้าง
- **Creator Sounds** - หน้าจัดการเพลงสำหรับผู้สร้าง

### Authentication Pages

- **Login** - หน้าเข้าสู่ระบบ
- **Register** - หน้าลงทะเบียน

## 🔌 API Integration

### Services Structure

```typescript
// src/services/https/index.tsx
export const authAPI = {
  loginMember: (data: LoginRequest) => axios.post("/member/auth", data),
  loginCreator: (data: LoginRequest) => axios.post("/creator/auth", data),
  // ...
};

export const soundAPI = {
  getAll: () => axios.get("/sounds"),
  getById: (id: number) => axios.get(`/sound/${id}`),
  create: (data: CreateSoundRequest) => axios.post("/new-sound", data),
  // ...
};
```

### Custom Hooks

```typescript
// src/hooks/useHistory.ts
export const useHistory = () => {
  const playSoundWithHistory = useCallback(async (soundId: number) => {
    // บันทึกประวัติการฟังเมื่อเล่นเพลง
  }, []);

  return { playSoundWithHistory };
};
```

## 🚀 การ Deploy

### Build สำหรับ Production

```bash
npm run build
```

ไฟล์ build จะอยู่ใน `dist/` folder

### Deploy บน Vercel

1. Push code ไปยัง GitHub
2. เชื่อมต่อกับ Vercel
3. Deploy อัตโนมัติ

### Deploy บน Netlify

1. Push code ไปยัง GitHub
2. เชื่อมต่อกับ Netlify
3. ตั้งค่า build command: `npm run build`
4. ตั้งค่า publish directory: `dist`

## 📱 Responsive Design

ระบบรองรับการแสดงผลบนอุปกรณ์ต่างๆ:

- **Desktop** - หน้าจอขนาดใหญ่
- **Tablet** - หน้าจอขนาดกลาง
- **Mobile** - หน้าจอขนาดเล็ก

## 🎨 UI/UX Features

- **Modern Design** - ใช้ Ant Design components
- **Responsive Layout** - รองรับทุกขนาดหน้าจอ
- **Loading States** - แสดงสถานะการโหลด
- **Error Handling** - จัดการข้อผิดพลาดอย่างเหมาะสม
- **User Feedback** - แสดงข้อความแจ้งเตือน
- **Smooth Animations** - การเคลื่อนไหวที่นุ่มนวล

## 🔒 State Management

- **Local Storage** - เก็บข้อมูลการ login และ user preferences
- **React State** - จัดการ state ของ components
- **Custom Hooks** - แชร์ logic ระหว่าง components

## 📊 Performance

- **Code Splitting** - แบ่ง code ตาม routes
- **Lazy Loading** - โหลด components เมื่อต้องการ
- **Optimized Images** - ใช้ Vite สำหรับ optimize images
- **Tree Shaking** - ลบ unused code ออก

## 🧪 Testing

### Unit Testing

```bash
npm run test
```

### E2E Testing

```bash
npm run test:e2e
```

## 📄 License

โปรเจคนี้เป็นส่วนหนึ่งของวิชา **System Analysis and Design 1/68**

## 👨‍💻 ผู้พัฒนา

**System Analysis and Design 1/68**

- TA รายวิชา System Analysis and Design

---

**หมายเหตุ**: ระบบนี้เป็นกรณีศึกษา สำหรับการใช้งานจริงควรเพิ่มฟีเจอร์ความปลอดภัยและการทดสอบเพิ่มเติม
